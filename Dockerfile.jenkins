# Lightweight Jenkins image with Docker support - Optimized for EC2
FROM jenkins/jenkins:lts-alpine

USER root

# Install Docker CLI only (not Docker daemon - use host Docker)
RUN apk add --no-cache \
    docker-cli \
    docker-compose \
    git \
    curl \
    bash

# Allow Jenkins to access host Docker socket
RUN addgroup jenkins docker

USER jenkins